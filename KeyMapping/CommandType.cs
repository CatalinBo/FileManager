namespace FileManager.KeyMapping
{
    /// <summary>
    /// Enumeration containing all types of commands that can be used in the <see cref="FileManager"/>.
    /// </summary>
    public enum CommandType
    {
        EndExecution,
        FileManipulation,
        Navigation,
        Confirmation,
        ShowInfo,
        AppFeatures
    }
}
